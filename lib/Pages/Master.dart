import 'package:flutter/material.dart';

class Master extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FlipFlop Aestro'),
      ),
      body: Center(
        child: Text('This is the Master page'),
      ),
    );
  }
}
