import 'package:flutter/material.dart';

class Install extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Install'),
      ),
      body: Center(
        child: Text('This is the Setup page'),
      ),
    );
  }
}
