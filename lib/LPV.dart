import 'package:flutter/material.dart';

class LPV extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('LPV'),
      ),
      body: Center(
        child: Text('This is the Leave Provision View page'),
      ),
    );
  }
}
