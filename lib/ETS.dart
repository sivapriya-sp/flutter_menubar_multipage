import 'package:flutter/material.dart';

class ETS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Time card Entry'),
      ),
      body: Center(
        child: Text('This is the Transactions page'),
      ),
    );
  }
}
